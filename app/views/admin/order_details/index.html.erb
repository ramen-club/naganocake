<h2>商品一覧画面</h2>

<table class="table table-hover table-inverse">
	<thead>
		<tr>
			<th>購入日時</th>
			<th>購入者</th>
			<th>注文個数</th>
			<th>注文ステータス</th>
        </tr>
	</thead>
	<tbody>
        <% @order_details.each do |order| %>
		<tr>
			<td><%= link_to order.created.at, admin_order_detail_path(@order_detail) %></td>
			<td><%= order.order.customer.family_name %><%= order.order.customer.first_name %></td>
			<td><%= item.count %></td>
			<td><%= item.order.order_status %></td>
			<td><%= item.sale_status %></td>
		</tr>
		<% end %>
	</tbody>
</table>

<%= paginate(@order_details) %>