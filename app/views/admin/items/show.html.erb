<ul>
    <li></li>
    <li><%= attachment_image_tag @item, :image, :fill, 60, 60, fallback: "no_image.jpg", size:'80x80' %></li>
    <li>商品名：<%= @item.name %></li>
    <li>商品説明：<%= @item.description %></li>
    <li>ジャンル：<%= @item.genre.name %></li>
    <li>税込価格（税抜価格）： <%= @item.price %></li>
    <li>販売ステータス：<%= @item.sale_status %></li>
</ul>